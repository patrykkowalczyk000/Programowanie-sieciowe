<% if (message.length > 0) { %>
	<div class="alert alert-danger start-panel">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
        <%= message %>
    </div>
<% } %>
